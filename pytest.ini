[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
testpaths = tests
timeout = 120
addopts = -m "not slow"
markers =
    slow: long-running tests that need network/time (run with: pytest -m slow)
    live: requires a live HTB/DVWA target (run with: pytest -m live)
